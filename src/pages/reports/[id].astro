---
// eslint-disable-next-line prettier/prettier
import MainLayout from "../../layouts/MainLayout.astro";
// import { ReportPage } from "../../components/test/ReportPage";

const { id } = Astro.params;
 // <ReportPage client:load id={id} />
if (!id) {
  return Astro.redirect("/404");
}
---

<MainLayout title="Raport testu | CacheTest PWA">
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-4">Raport szczegółowy</h1>
    <p class="mb-6 text-muted-foreground">Szczegółowa analiza wyników testu.</p>

   
     
  </div>
</MainLayout>

